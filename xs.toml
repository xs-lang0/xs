[package]
name = "xs"
version = "0.2.0"
xs_version = ">=1.0"
description = "The XS programming language"
license = "Apache-2.0"
authors = ["xs-lang0"]
repository = "https://github.com/xs-lang0/xs"

[dependencies]

[dev-dependencies]

[build]
entry = "src/main.c"
targets = ["native", "wasm32", "wasi"]
